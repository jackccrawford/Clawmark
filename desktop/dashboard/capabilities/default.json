{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability: main window can call all stub commands",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
